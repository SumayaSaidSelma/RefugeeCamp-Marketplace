<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= item.name %></title>
    <link rel="stylesheet" href="/item.css">
</head>

<body>

    <header>
        <a href="/items/new" class="btn post-item">Post a New Item</a>
        <a href="/logout" class="btn logout">Log Out</a>
    </header>

    <main class="item-page">

        <div class="item-image">
            <img src="<%= item.image %>" alt="<%= item.name %>">
        </div>

        <h1><%= item.name %></h1>
        <p>Price: $<%= item.price %></p>
        <p>Description: <%= item.description %></p>
        <p>Size: <%= item.size %></p>
        <p>Condition: <%= item.condition %></p>
        <p>Location: <%= item.location %></p>

        <div class="buttons">
            <button class="btn" onclick="alert('Not available')">Ask</button>
            <button class="btn" onclick="alert('This feature is not available now')">Share Location</button>
            <button class="btn buy" onclick="alert('You don\'t have money to buy anything, get a job')">Buy</button>
        </div>
        
        <div class="delete-button">
            <form action="/items/<%= item._id %>?_method=DELETE" method="POST">
                <button type="submit" class="btn delete">Delete</button>
    </main>
</body>
</html>

