<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= item.name %></title>
    <link rel="stylesheet" href="/item.css">
</head>

<body>

    <header>
        <a href="/items/new" class="btn post-item">Post a New Item</a>
        <a href="/" class="a goback" onclick="alert('Sorry cant go out yet!')">Back to Home</a>
    </header>

    <main class="item-page">

        <div class="item-image">
            <img src="<%= item.image %>" alt="<%= item.name %>">
        </div>

        <h1><%= item.name %></h1>
        <p>Price: $<%= item.price %></p>
        <p>Description: <%= item.description %></p>
        <p>Size: <%= item.size %></p>
        <p>Condition: <%= item.condition %></p>
        <p>Location: <%= item.location %></p>


        <div class="buttons">
            <button class="btn" onclick="alert('Ask google! This teature is not available')">Ask</button>
            <button class="btn" onclick="alert('Save your self! This feature is not available now')">Share Location</button>
            <button class="btn buy" onclick="alert('You don\'t have money to buy anything, get a job!')">Buy</button>
            <a href="/items/<%= item._id %>/edit" class="btn update">Update Item</a>            
            
            <form action="/items/<%= item._id %>?_method=DELETE" method="POST">
                <button type="submit" class="btn delete">Delete</button>
            </form>
            
        </div>


        
        
    </main>
</body>
</html>

